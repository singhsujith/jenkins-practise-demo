pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }

        stage('Slack') {
            steps {
                slackSend message: 'textmessage'
            }
        }
    }
}
